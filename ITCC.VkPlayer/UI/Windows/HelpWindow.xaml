<Window x:Class="ITCC.VkPlayer.UI.Windows.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Справка" Height="480" Width="640"
        ResizeMode="NoResize"
        Closed="HelpWindow_OnClosed">
    <Grid>
        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button x:Name="OpenLogWindowButton"
                    Grid.Row="0"
                    Grid.Column="0"
                    Content="Окно лога"
                    HorizontalAlignment="Right"
                    Click="OpenLogWindowButton_OnClick" />

            <Button x:Name="CloseHelpButton"
                    Grid.Row="2"
                    Grid.Column="0"
                    Content="Закрыть"
                    HorizontalAlignment="Right"
                    ToolTip="Закрытие этого окна"
                    Click="CloseHelpButton_OnClick" />
        </Grid>

    </Grid>
</Window>